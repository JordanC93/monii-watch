node_modules
dist
dist-ssr
*.local
.env
.env.local
.DS_Store
.idea
.vscode/*
!.vscode/extensions.json
*.log
.cache

# Tauri build artifacts
src-tauri/target
src-tauri/gen

# TypeScript incremental build cache
*.tsbuildinfo

# Capacitor — generated native projects + Pods cache.
# Generated by `npm run cap:add:ios` / `cap add android` and meant to be
# regenerated locally rather than checked in (mirrors how src-tauri/gen
# works for Tauri). Future maintainer can flip these if they want to
# pin native source.
ios
android
