{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Cross-platform capabilities, applies to every target (desktop, iOS, Android). Window labels: `main` is the original window; `cb-*` is any extra window opened via cmd_open_new_window so multi-window users get the same permissions as the main window.",
  "windows": ["main", "cb-*"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "dialog:default",
    "fs:default",
    "fs:allow-read-text-file",
    "fs:allow-write-text-file",
    "fs:allow-mkdir",
    "fs:allow-exists",
    "fs:allow-read-dir",
    {
      "identifier": "fs:scope",
      "allow": [
        { "path": "$HOME/Library/Mobile Documents/com~apple~CloudDocs/Monii/**" },
        { "path": "$HOME/iCloud Drive/Monii/**" }
      ]
    }
  ]
}
