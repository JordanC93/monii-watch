{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Desktop-only capabilities — references plugins we cfg-gate to non-mobile targets in Cargo.toml + lib.rs (updater, process). Scoped via `platforms` so iOS / Android builds skip it; otherwise the permission references resolve to nothing and Cargo fails.",
  "platforms": ["macOS", "windows", "linux"],
  "windows": ["main"],
  "permissions": [
    "updater:default",
    "process:default",
    "process:allow-restart"
  ]
}
